<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Embroidery: <%= currentPage.title %> Example</title>
  <link rel="stylesheet" href="/main.css">
  <script src="/main.js" defer></script>
</head>

<body>
  <header>
    <h1 class="title">Embroidery</h1> <h3>Examples</h3>
  </header>
  <main>
    <aside>
      <ul class="nav">
        <% pages.forEach(page => { %>
          <li>
            <a href="<%= page.path %>" class="<%= page.path == currentPage.path ? " active " : " " %>">
              <%= page.title %>
            </a>
          </li>
          <% }) %>
      </ul>
    </aside>

    <div class="container">
